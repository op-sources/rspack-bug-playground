
import {TableColumnProps} from '../interface';

export interface InnerTableColumnProps extends TableColumnProps {
    renderTitle?: (column: InnerTableColumnProps) => void;
    children?: InnerTableColumnProps[];
};

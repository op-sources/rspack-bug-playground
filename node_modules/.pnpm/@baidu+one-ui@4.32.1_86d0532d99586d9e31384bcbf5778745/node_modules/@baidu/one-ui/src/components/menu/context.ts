import {createContext} from 'react';
import {MenuProps} from './interface';

type MenuContextProps = Pick<MenuProps, 'inlineCollapsed' | 'inlineIndent' | 'inlinePaddingLeft'>;

export default createContext<MenuContextProps>({});

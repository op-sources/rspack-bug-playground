import React from 'react';
import {classnames} from '../../core/commonTools';

const Divider = ({
    className = undefined,
    prefixCls = 'one-menu',
    style = undefined
}) => (
    <li
        className={classnames(className, `${prefixCls}-item-divider`)}
        style={style}
    />
);

export default Divider;

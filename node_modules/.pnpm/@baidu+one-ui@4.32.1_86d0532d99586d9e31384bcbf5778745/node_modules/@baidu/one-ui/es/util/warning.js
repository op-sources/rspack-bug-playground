import warning from 'warning';
export var propChangeWarning = function propChangeWarning(component, props, keyMap) {
  Object.keys(keyMap).forEach(function (key) {
    warning(!(key in props), "The `" + key + "` prop of `" + component + "` is deprecated, use `" + keyMap[key] + "` instead'");
  });
};
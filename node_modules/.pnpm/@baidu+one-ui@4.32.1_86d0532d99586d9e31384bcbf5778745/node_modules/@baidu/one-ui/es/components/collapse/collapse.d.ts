import { PureComponent } from 'react';
import { CollapseProps } from './interface';
import Panel from './panel';
interface CollapseState {
    activeKey: string[];
}
declare class Collapse extends PureComponent<CollapseProps, CollapseState> {
    static defaultProps: {
        prefixCls: string;
        size: string;
        onChange: (...args: any[]) => void;
        type: string;
        showExpandIcon: boolean;
        expandIconPosition: string;
    };
    static Panel: typeof Panel;
    constructor(props: CollapseProps);
    static getDerivedStateFromProps(nextProps: CollapseProps): {
        activeKey: string[];
    };
    onClickItem: (key: string) => void;
    renderPanel: () => any;
    /**
      * 归一化边框
      *
      * 不同`type`默认情况如下：
      * normal: Y(不可配)
      * simple: Y(可配)
      * basic: N(可配)
      * strong: N(可配)
      *
      * @param {string} type 类型
      * @param {bool} bordered 边框
      * @return {bool} 是否有边框
     */
    normalizedBordered(type: string, bordered: boolean): boolean;
    /**
      * 归一化是否响应交互
      *
      * 不同`type`默认情况如下：
      * normal: N(不可配)
      * simple: Y(不可配)
      * basic: N(可配)
      * strong: N(可配)
      *
      * @param {string} type 类型
      * @param {bool} dull 边框
      * @return {bool} 是否响应交互
     */
    normalizedDull(type: string, dull: boolean): boolean;
    render(): JSX.Element;
}
export default Collapse;

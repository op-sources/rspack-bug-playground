import { PureComponent, ReactNode } from 'react';
interface ScrollContainerProps {
    title: ReactNode[];
    content: ReactNode[];
    prefixCls: string;
    activeKey: string;
}
export default class ScrollContainer extends PureComponent<ScrollContainerProps> {
    render(): JSX.Element;
}
export {};

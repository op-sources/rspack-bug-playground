import { PureComponent } from 'react';
import { CheckboxGroupContext } from './context';
import { CheckboxButtonProps } from './interface';
declare class CheckboxButton extends PureComponent<CheckboxButtonProps> {
    static defaultProps: {
        prefixCls: string;
    };
    render(): JSX.Element;
    renderCheckbox(context: CheckboxGroupContext): JSX.Element;
}
export default CheckboxButton;

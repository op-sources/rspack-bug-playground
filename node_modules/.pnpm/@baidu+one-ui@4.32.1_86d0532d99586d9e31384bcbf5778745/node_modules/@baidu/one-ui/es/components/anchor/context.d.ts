import React, { ReactNode, MouseEvent } from 'react';
export interface AnchorContext {
    registerLink(link: string): any;
    unregisterLink(link: string): any;
    activeLink: string;
    scrollTo(link: string): any;
    onClick(e: MouseEvent, link: {
        title: ReactNode;
        href: string;
    }): any;
}
declare const _default: React.Context<AnchorContext>;
export default _default;

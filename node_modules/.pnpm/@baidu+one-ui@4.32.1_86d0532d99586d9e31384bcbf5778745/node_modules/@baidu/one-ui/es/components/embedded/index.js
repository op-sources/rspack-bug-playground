import _extends from "@babel/runtime/helpers/extends";
import _objectWithoutPropertiesLoose from "@babel/runtime/helpers/objectWithoutPropertiesLoose";
import React from 'react';
import PropTypes from 'prop-types';
import { Consumer } from '../providerConfig/context';
import { formatCommonConfig } from '../../core/commonTools';
import Embedded from './embedded';
import Horizon from './horizon';
/**
 * @deprecated
 */

function EmbeddedContainer(props) {
  var suffixCls = props.suffixCls,
      restProps = _objectWithoutPropertiesLoose(props, ["suffixCls"]);

  return /*#__PURE__*/React.createElement(Consumer, null, function (consumerConfig) {
    var _formatCommonConfig = formatCommonConfig(_extends({}, props, {
      suffixCls: suffixCls
    }), consumerConfig),
        prefixCls = _formatCommonConfig.prefixCls,
        size = _formatCommonConfig.size;

    return /*#__PURE__*/React.createElement(Embedded, _extends({}, restProps, {
      size: size,
      prefixCls: prefixCls
    }));
  });
}

EmbeddedContainer.propTypes = {
  suffixCls: PropTypes.string
};
EmbeddedContainer.defaultProps = {
  suffixCls: 'embedded'
};
/**
 * @deprecated
 */

function HorizonContainer(props) {
  var suffixCls = props.suffixCls,
      restProps = _objectWithoutPropertiesLoose(props, ["suffixCls"]);

  return /*#__PURE__*/React.createElement(Consumer, null, function (consumerConfig) {
    var _formatCommonConfig2 = formatCommonConfig(_extends({}, props, {
      suffixCls: suffixCls
    }), consumerConfig),
        prefixCls = _formatCommonConfig2.prefixCls,
        size = _formatCommonConfig2.size;

    return /*#__PURE__*/React.createElement(Horizon, _extends({}, restProps, {
      size: size,
      prefixCls: prefixCls
    }));
  });
}

HorizonContainer.propTypes = {
  suffixCls: PropTypes.string
};
HorizonContainer.defaultProps = {
  suffixCls: 'embedded-horizon'
};
EmbeddedContainer.Horizon = HorizonContainer;
export default EmbeddedContainer;
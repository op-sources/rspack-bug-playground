import { PureComponent, ReactElement } from 'react';
import { UploaderImageItemProps, UploadImageActionItem } from '../interface';
export default class ImageItem extends PureComponent<UploaderImageItemProps> {
    static defaultProps: {
        thumbUrl: string;
        type: string;
        showUploadListIcon: {
            showPreviewIcon: boolean;
            showReUploadIcon: boolean;
            showRemoveOnIcon: boolean;
        };
    };
    state: {
        visible: boolean;
    };
    renderItemOverlay: (item: UploadImageActionItem) => JSX.Element;
    onVisibleChange: (visible: boolean) => void;
    renderOptionIcon: () => JSX.Element;
    renderOpertaion: (iconsSet: ReactElement) => {};
    renderHelper(): JSX.Element;
    render(): JSX.Element;
}

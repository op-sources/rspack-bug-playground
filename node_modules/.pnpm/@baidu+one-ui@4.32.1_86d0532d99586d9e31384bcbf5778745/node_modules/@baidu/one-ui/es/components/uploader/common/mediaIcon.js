import React from 'react';
import { IconImageAdd, IconVideoAdd, IconMediaAdd, IconImageSolid, IconVideoSolid, IconVideoFilmSolid } from 'dls-icons-react';
import { THEME_LIGHT_D22 } from '../../config';
var icons = {
  image: IconImageAdd,
  video: IconVideoAdd,
  media: IconMediaAdd
};
var d22Icons = {
  image: IconImageSolid,
  video: IconVideoSolid,
  media: IconVideoFilmSolid
};
export default (function (_ref) {
  var theme = _ref.theme,
      type = _ref.type,
      className = _ref.className;
  var Icon = (theme === THEME_LIGHT_D22 ? d22Icons : icons)[type];

  if (!Icon) {
    return null;
  }

  return /*#__PURE__*/React.createElement(Icon, {
    className: className
  });
});
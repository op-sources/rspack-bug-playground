import { PureComponent } from 'react';
interface ShortCutProps {
    shortcuts?: {
        label: string;
        from: number | {
            startOf: 'month' | 'week' | 'day';
            months?: number;
            weeks?: number;
            days?: number;
        };
        to: number | {
            startOf: 'month' | 'week' | 'day';
            months?: number;
            weeks?: number;
            days?: number;
        };
    }[];
    prefixCls?: string;
    dateFormat?: string;
    onChange(value: string[]): void;
}
export default class ShortCut extends PureComponent<ShortCutProps> {
    static defaultProps: {
        onChange(): void;
    };
    onClickShortCut: (index: number) => void;
    render(): JSX.Element;
}
export {};

import { ReactNode } from 'react';
import { TooltipProps } from '../tooltip';
export interface PopoverProps extends Omit<TooltipProps, 'type'> {
    /**
     * 内容
     */
    content?: ReactNode;
    /**
     * 尺寸
     */
    size?: 'small' | 'medium';
}

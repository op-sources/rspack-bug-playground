import _extends from "@babel/runtime/helpers/extends";
import React from 'react';
import PropTypes from 'prop-types';
import omit from 'omit.js';
import { Consumer } from '../providerConfig/context';
import { formatCommonConfig, classnames } from '../../core/commonTools';
import { iconSvgMap } from '../../core/iconSvgTools';
export default function IconSvg(props) {
  var className = props.className,
      suffixCls = props.suffixCls,
      type = props.type;
  return /*#__PURE__*/React.createElement(Consumer, null, function (consumerConfig) {
    var _formatCommonConfig = formatCommonConfig(_extends({}, props, {
      suffixCls: suffixCls
    }), consumerConfig),
        prefixCls = _formatCommonConfig.prefixCls;

    var classString = classnames(prefixCls, prefixCls + "-" + type, className);
    var Comp = iconSvgMap[type];

    if (!Comp) {
      return null;
    }

    return /*#__PURE__*/React.createElement(Comp, _extends({}, omit(props, ['type', 'prefixCls', 'suffixCls']), {
      className: classString
    }));
  });
}
IconSvg.propTypes = {
  type: PropTypes.string.isRequired,
  className: PropTypes.string,
  prefixCls: PropTypes.string,
  suffixCls: PropTypes.string
};
IconSvg.defaultProps = {
  className: '',
  suffixCls: 'iconSvg'
};
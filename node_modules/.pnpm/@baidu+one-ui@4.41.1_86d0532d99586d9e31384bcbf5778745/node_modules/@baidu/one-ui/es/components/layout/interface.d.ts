import { BasePropsWithChildren } from '../interface';
export interface LayoutProps extends BasePropsWithChildren {
    /**
     * 布局方向，SSR不符合预期时，可强制指定
     */
    direction?: 'row' | 'column';
}
export interface LayoutHeaderProps extends BasePropsWithChildren {
    /**
     * 固定
     */
    sticky?: boolean;
}
export declare type LayoutContentProps = BasePropsWithChildren;
export interface LayoutFooterProps extends BasePropsWithChildren {
    /**
     * 固定
     */
    sticky?: boolean;
}
export interface LayoutSidebarProps extends BasePropsWithChildren {
    /**
     * 宽度
     */
    width?: number | string;
    /**
     * 是否展示默认展开/收起
     */
    showToggle?: boolean;
    /**
     * 收起状态
     */
    defaultCollapsed?: boolean;
    /**
     * 收起状态, 受控
     */
    collapsed?: boolean;
    /**
     * 收起类型
     */
    collapseMode?: 'slim' | 'hidden';
    /**
     * 展开/收起回调, auto表示自动还是手动
     */
    onCollapsedChange?(collapsed: boolean, auto: boolean): void;
    /**
     * 窗口小于1248时，自动收起
     */
    autoCollapse?: boolean;
    /**
     * 固定
     */
    sticky?: boolean;
}

/// <reference types="react" />
import { LightboxToolbarActionItem } from '../interface';
export interface LightboxToolbarProps {
    isSupportZoom?: boolean;
    isShowIndicator?: boolean;
    slide: number;
    total: number;
    prefixCls: string;
    scale: number;
    minScale: number;
    maxScale: number;
    actionItems: LightboxToolbarActionItem[];
    onClick: (key: LightboxToolbarActionItem['key']) => void;
}
export default function Toolbar({ prefixCls, actionItems, scale, minScale, maxScale, isSupportZoom, isShowIndicator, slide, total, onClick }: LightboxToolbarProps): JSX.Element;
